name: combisv3
description: "Public transport route explorer app for Tlaxcala, Mexico."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:  
    sdk: flutter
  google_fonts: ^6.2.1
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  sqflite: ^2.4.1
  sqflite_common_ffi: ^2.3.6
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
